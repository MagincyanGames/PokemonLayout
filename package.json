{
  "name": "pokemonlayout",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "parse": "npx ts-node ./parse-pokemon/parser.ts",

    "install": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "node scripts/buid.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.2.3",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "fs": "^0.0.1-security",
    "http-proxy-middleware": "^3.0.5",
    "path": "^0.12.7"
  }
}
